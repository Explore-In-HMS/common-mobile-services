apply from: "$project.rootDir/base_sdk.gradle"
def libs = project.libs

android {
    namespace project.namespace + ".texttospeech"
}

dependencies {
    implementation "com.huawei.hms:ml-computer-voice-tts:${libs.hms.textToSpeech}"
    api project(":core")
}

