apply from: "$project.rootDir/base_sdk.gradle"
def libs = project.libs

android {
    namespace project.namespace + ".creditcardscanner"
}

dependencies {
    api "com.huawei.hms:ml-computer-card-bcr:${libs.hms.cardBcr}"
    api project(":core")
}

