apply from: "$project.rootDir/base_sdk.gradle"
def libs = project.libs

android {
    namespace project.namespace + ".identity"
}

dependencies {
    api project(path: ":core")

    implementation "com.huawei.hms:identity:${libs.hms.identity}"
}